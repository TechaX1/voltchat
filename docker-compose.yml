services:
  frontend:
    build: .
    container_name: voltchat  # <--- Remember this name!
    restart: always
    ports:
      - "8026:80"  # host:container — the built app is served by nginx on port 80
